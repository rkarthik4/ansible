- name: p1 - create user account in all managed node
  hosts: allservers
  tasks:
    - name: create user sales1
      ansible.builtin.user:
        name: sales1
        state: present
        comment: "sales manager"
#  ansible-playbook ./2.usercreate.yaml --syntax-check
#  ansible-playbook ./2.usercreate.yaml